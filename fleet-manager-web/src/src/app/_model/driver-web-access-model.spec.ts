import { DriverWebAccessModel } from './driver-web-access-model';

describe('DriverWebAccessModel', () => {
  it('should create an instance', () => {
    expect(new DriverWebAccessModel()).toBeTruthy();
  });
});

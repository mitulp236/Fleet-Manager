import { BudgetCategoryModel } from './budget-category-model';

describe('BudgetCategoryModel', () => {
  it('should create an instance', () => {
    expect(new BudgetCategoryModel()).toBeTruthy();
  });
});
